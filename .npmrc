# npm 11.3 crashes resolving this tree — a TypeError inside its peer walker,
# not a real conflict. Legacy mode skips that walk. Set here rather than passed
# as a flag so `npm ci` resolves the same way `npm install` did, which it must
# for the lockfile to match.
legacy-peer-deps=true
