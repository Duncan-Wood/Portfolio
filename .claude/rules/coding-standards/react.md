---
paths:
  - "src/**/*.jsx"
  - "src/**/*.js"
---
# React coding standards

The standards listed below are not loaded automatically. Before writing or changing code in this file, decide whether the current change falls under one of them; if it does, open that standard's full text with the Read tool first. If none is relevant, skip them — do not open a standard you do not need.

## Available standards

- [React Responsive Layout: Adapt Automatically, Don't Hand-Tune Breakpoints](../../../docs/coding-standards/react-responsive-layout.md) — How to build layout markup that reflows on its own instead of being tuned to a specific width. Read when adding or changing any multi-element layout (a row, grid, nav, list, or card set) or when touching responsive or breakpoint utility classes.
